#include <iostream>
using namespace std;

template <typename T>
struct Node {
    T val;
    Node<T> *next;
};

void insert(Node<T> **head, int n) {
    if(*node == NULL) {
        *node = new 
    }
}

int main() {

}